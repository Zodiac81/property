/////////////////				contentType

	Число «number»
		
			var n = 123;
			n = 12.345;

	Строка «string»
		
		в js строка приоритетнее

			var str = "Мама мыла раму";

	Булевый (логический) тип «boolean»

			var checked = true; // поле формы помечено галочкой

	Специальное значение «null»

			var age = null;

	Специальное значение «undefined»

			var x;
			alert( x ); // выведет "undefined"
			x = undefined;

	Объекты «object»

			var user = { name: "Вася" };
			
/////////////////////////////////////////////////////////////////////////////////////////////////////////////

	100 && 'hello'	// return 'hello'
	'hello' || null	// return null

/////////////////////////////////////////////////////////////////////////////////////////////////////////////

typeof			//	typeof 123;
isNaN			//	isNaN('asd');
parseInt		//	parseInt('123sdf');
parseFloat		//	parseFloat('123.123gdfg');
var x = Number(true);
var x = Boolean(true);
var x = String(true);
/////////////////////////////////////////////////////////////////////////////////////////////////////////////

/////////////////////////////////////////////////////////////////////////////////////////////////////////////
